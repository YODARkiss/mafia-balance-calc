# Документация калькулятора «Стол»

Версия: 2026-08-28. Якорь силы — ФИИМ 10/3.

## Что считает калькулятор

Две оси, не одна:

1. **Сила города `p`** — кто чаще дотягивает, если партия живёт.
2. **Тайминг** — средние оплаченные раунды `billR` и минуты.

Сила «примерно ровно» при `billR < 3` — ловушка: город не успевает говорить.

## Якорь силы

`p = 0` на спорте ФИИМ: 10 игроков, 3 мафии с доном, шериф только проверяет, карты закрыты, нет путаны / доктора / третьей стороны.

Чёрные по столу: `clubM(N)` = 2 (≤9), 3 (10–12), 4 (13–16), 5 (17+).

```
p = clubM(N) − M
  + шериф: выстрел +0.50, проверка 0, нет шерифа −0.40
  + путана (см. ниже)
  + доктор +0.38 или +0.22 если путана уже есть
  + везунчик +0.20
  + без дона +0.15 (дон в якоре = 0)
  + маньяк −0.45 / отравитель −0.25 / якудза −0.70
  + оглашать роли +0.35 (закрытые карты в якоре = 0)
```

Бейдж: `p > 0.55` город жирнее, `p < −0.55` под прессом, иначе ровно. Жирный город — хорошо новичкам.

## Тайминг

`T = N − M − third` (мирные слоты).

- `minR = ceil((T−M) / (2+extra))` — быстрый конец, если все пули в город.
- `parity = T − M` — паритет мафии при одной ночной пуле.
- `extra = 1` если маньяк/якудза (вторая утренняя пуля). У отравителя `extra = 0`.
- `raw` стартует с `parity`, плюс роли, минус вторая пуля **−1.85**.
- `billR = ceilHalf(raw)` — нельзя сыграть 0.2 раунда: 3.2 → 3.5.
- Минуты считаются по `ceil(billR)` дням речей.
- Живые на дне `d`: `N − 1 − extra − (d−1)×(2+extra)`. Маньяк реально снимает второго.

Градация по `billR`: блиц <2.5, коротко <3.5, норма ≤5.5, длинно ≤7, иначе марафон. Кризис общения: `billR < 3`.

## Маньяк вместо одной мафии

Кнопка 🔪 сравнивает `M` чёрных vs `(M−1) + маньяк`. Клик сам снимает одну мафию.

На 12 с шерифом-выстрелом и путаной-блоком:

| Стол | billR | мин |
|---|---|---|
| 4 мафии | 3.5 | 53 |
| 3 + маньяк | 3.0 | 39 |
| 3 без третьей | 5.5 | 68 |

Раньше минуты не вычитали второго трупа — 12/3+🔪 выходили длиннее 12/4. Это было ошибкой.

Якудза (🔪🔪): второй слот с мирного. Одна командная пуля, как у маньяка.

Отравитель: нет второй утренней пули, жертва говорит и голосует. Сила города ≈ +0.20 vs маньяка, тайминг длиннее.

## Путана

Не ходит к себе и не ходит к одному дважды (качественно, не в Monte-Carlo).

- **блок команды** — глушит выстрел той мафии, к кому пришла. При M≥2 командный выстрел падает, если цель в команде.
- **алиби** — блок + днём цель нельзя повесить. Если стреляют путану — с ней уходит посещённый.
- **лечение+блок одного** — сейвит цель, но командный выстрел при M≥2 идёт.

С маньяком два трупа ≈ «цель мирный»; один труп ≈ скорее закрыла чёрного. У отравителя двух утренних трупов нет.

## Роли и новички

Счётчик разных имён за столом. ≤4 просто, ≤6 норм, ≥7 густо. Дон — первый кандидат на вылет новичкам.

## Чего калькулятор не делает

- Не предсказывает % побед (нет Monte-Carlo стола).
- Не моделирует повтор визита доктора/путаны по раундам числом — только качественно.
- Не считает «качели» отдельным индексом: смотрите размах min–max минут.
- Скрытое число мафии — вилка M / M+1, не совет по умолчанию.

# Документация калькулятора «Стол»

Версия: 2026-08-29. Якорь силы — ФИИМ 10/3 (`p = 0`).

Калькулятор не считает % побед. Две оси:

| Ось | Что значит | Единица |
|---|---|---|
| **Сила города `p`** | Кто чаще дотягивает, *если партия живёт* | условные веса, якорь 0 |
| **Тайминг** | Сколько кругов общения и минут | раунды `billR`, минуты |

`p ≈ 0` при `billR < 3` — ловушка: город «ровный», но не успевает говорить.

---

## Якорь

`p = 0` на спорте ФИИМ:

- 10 игроков, 3 мафии **с доном**
- шериф **только проверяет** (не стреляет)
- карты **закрыты**
- нет путаны, доктора, везунчика, третьей стороны

Чёрные «по столу» `clubM(N)`:

| N | clubM |
|---|---|
| ≤ 9 | 2 |
| 10–12 | 3 |
| 13–16 | 4 |
| ≥ 17 | 5 |

Лишняя мафия относительно якоря: **сила −1.0** за каждую. Нехватка: **+1.0**.

Бейдж силы:

| `p` | Бейдж |
|---|---|
| > +0.55 | город жирнее (новичкам ок) |
| −0.55 … +0.55 | примерно ровно |
| < −0.55 | город под прессом |

---

## Веса силы (`p`)

Знак «+» — городу легче. «−» — городу тяжелее.

### Роли и модификации

| Роль / тумблер | Сила города | Комментарий |
|---|---|---|
| 🖤 каждая мафия сверх `clubM` | **−1.00** | якорь 10–12 = 3 чёрных |
| 🖤 каждая мафия меньше `clubM` | **+1.00** | |
| 🔫 шериф, только проверка | **0** | якорь ФИИМ |
| 🔫 шериф **стреляет** | **+0.50** | к якорю проверки |
| 🔫 **нет шерифа** | **−0.40** | город слепой |
| 🎩 дон есть | **0** | якорь ФИИМ |
| 🎩 **без дона** | **+0.15** | мафия без поисковика |
| 💋 путана | см. таблицу ниже | зависит от режима и третьей стороны |
| 🩺 доктор (без путаны) | **+0.38** | |
| 🩺 доктор **вместе с путаной** | **+0.22** | не копим защиту |
| 🍀 везунчик | **+0.20** | один сейв, не «вторая жизнь стола» |
| 🔪 маньяк | **−0.45** | бьёт и красных |
| 🧪 отравитель (вместо маньяка) | **−0.25** | vs маньяка городу **+0.20** |
| 🔪🔪 якудза (2 шт) | **−0.70** | |
| 🃏 роли **оглашают** | **+0.35** | |
| 🃏 карты закрыты | **0** | якорь ФИИМ |

Маньяк в калькуляторе **заменяет одну мафию**. Сравнение 12/4 vs 12/3+🔪: −1 чёрная (**+1.00**) и маньяк (**−0.45**) → городу **+0.55**, плюс вторая пуля режет тайминг.

### Путана — сила

`pM = M / (N−1)` — шанс ткнуть в мафию (себя не ходит).  
`pTwo = (N−M−2) / (N−1)` — шанс, что при двух трупах цель мирный. Считается **только если есть маньяк** (не отравитель).

| Режим | Без третьей стороны | С маньяком | С отравителем |
|---|---|---|---|
| нет | 0 | 0 | 0 |
| **блок команды** | `0.30 + pM×0.65` | `0.25 + pTwo×0.45` | `0.25` (двух трупов нет) |
| **блок + алиби** | `0.55 + pM×0.70` | `0.45 + pTwo×0.50` | как без третьей |
| **лечение + блок одного** | `0.32` если M≥2, иначе `0.30 + pM×0.55` | `0.28 + pTwo×0.20` если M≥2 | как без третьей при M≥2: `0.32` |

Пример на 12 игроках, 3 мафии (`pM ≈ 0.27`, `pTwo ≈ 0.64`):

| Режим | 12/3 | 12/3+🔪 | 12/3+🧪 |
|---|---|---|---|
| блок команды | +0.48 | +0.54 | +0.25 |
| блок + алиби | +0.74 | +0.77 | +0.74 |
| лечение + блок | +0.32 | +0.41 | +0.32 |

Механика:

- **блок команды** — глушит выстрел той мафии, к кому пришла. При M≥2 командный слот падает.
- **алиби** — плюс днём цель нельзя повесить. Выстрел в путану уносит и посещённого.
- **лечение+блок одного** — сейвит цель; командный выстрел при M≥2 **идёт**.

Не ходит к себе и не ходит к одному дважды (качественно, не в симуляции по раундам).

---

## Веса тайминга (раунды)

`T = N − M − third` — мирные слоты (шериф/путана/доктор/везунчик внутри T).

```
minR   = ceil( (T−M) / (2 + extra) )     # быстрый конец, все пули в город
parity = T − M                            # паритет мафии при 1 ночной пуле
extra  = 1  если маньяк или якудза
       = 0  если отравитель или нет третьей

raw = parity
    + путана.time
    + доктор
    + везунчик
    + шериф
    + третья сторона
    + закрытые карты
raw ∈ [minR, maxR]
billR = ceilHalf(raw)    # 3.2 → 3.5; нельзя сыграть кусок раунда
days  = ceil(billR)
```

| Модификатор | Δ раундов (`raw`) | Зачем |
|---|---|---|
| база | `parity = T−M` | сколько голосований до паритета при 1 пуле |
| 💋 путана, блок, без маньяка | `pM × 1.15` | тихие ночи |
| 💋 путана, блок, с маньяком | `0.40 + pM×0.20` | вторая пуля реже глушится |
| 💋 путана, алиби | `0.50 + сила_путаны×0.35` | ещё и день без повешения цели |
| 💋 путана, лечение, M≥2 | **+0.35** | |
| 💋 путана, лечение, M=1 | **+0.50** | может снять единственный выстрел |
| 🩺 доктор | **+0.40** | |
| 🍀 везунчик | **+0.50** | |
| 🔫 шериф стреляет | **−1.05** | ночная пуля города |
| 🔫 шериф только проверяет | **−0.15** | проверки чуть ускоряют чтение |
| нет шерифа | 0 к раундам | |
| 🔪 вторая утренняя пуля (маньяк/якудза) | **−1.85** | главный рычаг блица |
| 🧪 отравитель (нет утреннего трупа) | **−0.50** | vs маньяка **+1.35** раунда |
| 🃏 карты закрыты | **+0.35** | |
| 🃏 оглашать роли | 0 | якорь «нашего» стола по времени |

Градация по **средним** `billR` (не по `minR`):

| billR | Тайминг |
|---|---|
| < 2.5 | блиц |
| < 3.5 | коротко |
| ≤ 5.5 | норма |
| ≤ 7 | длинно |
| > 7 | марафон |

Кризис общения: **`billR < 3`**. Смотрите средние, не быстрый конец мафии.

### Потолок `maxR` (stall)

Не сила и не среднее. Верхняя оценка «если ночи вязнут»:

| Источник | Δ к потолку |
|---|---|
| 🍀 везунчик | +1.0 |
| 🩺 доктор | +1.9 |
| путана при extra | +0.45 |
| extra и есть мафия | +0.30 |
| путана-алиби | +0.50 |
| путана-лечение | +0.25 |
| карты закрыты | +0.35 |
| отравитель | +0.40 |
| константа | +0.80 |
| крышка | `N−3` голосований |

---

## Минуты

Считаются по **оплаченным** дням `days = ceil(billR)`, не по дробному `raw`.

Живые на круге дня `d` (после ночи):

```
speakers(d) = max(3, N − 1 − extra − (d−1)×(2 + extra))
```

Маньяк снимает **второго** за ночь. Раньше минуты это игнорировали — 12/3+🔪 выходили длиннее 12/4.

```
секунды = 80
        + Σ speakers(d) × речь
        + балаган × 120 × days     # если включён
        + days × 165               # голосование
        + (days+1) × ns            # ночи, включая нулевую
минуты = round(секунды / 60)
```

Ночь `ns` (секунды действий):

| Слот | +с |
|---|---|
| база | 62 |
| путана | +15 |
| дон | +10 |
| доктор | +15 |
| шериф | +18 |
| маньяк / отравитель / якудза | +18 |

Балаган: **+2 мин каждый день**, колоду не трогает.

На экране: крупные минуты = ожидание (`tExp` по `billR`), мелким `min–max` по быстрому концу и потолку.

---

## Примеры (12 игроков, шериф стреляет, дон, карты открыты)

| Стол | p | billR | мин |
|---|---|---|---|
| 3 мафии, без третьей, путана-блок | +1.33 жирнее | 5.5 | ~68 |
| 3 + маньяк, путана-блок | +0.94 жирнее | 3.0 | ~39 |
| 4 мафии, путана-блок | +0.39 ровно | 3.5 | ~53 |
| ФИИМ 10/3, проверка, карты закрыты | **0** | 4.5 | ~51 |

Маньяк вместо 1 мафии на этом столе: сила **+0.55**, время **−14 мин**.

---

## Наборы правил

| Набор | Что ставит |
|---|---|
| 🏁 Спорт | N=10, M=3, дон, шериф-проверка, карты закрыты, без специй. `p=0` |
| 🏙️ Синдикат | clubM(N), шериф-проверка, путана-алиби с 9, доктор с 13, маньяк с 9, карты открыты |
| 🏠 Наш | шериф стреляет, путана-блок с 9, доктор не обязателен |

---

## Роли новичкам

Число **разных имён** за столом (мафия, дон, маньяк, шериф, путана, доктор, везунчик, мирные — каждое по счётчику).

| Уникальных | Оценка |
|---|---|
| ≤ 4 | просто новичкам |
| 5–6 | норм. новичкам |
| ≥ 7 | густо: уберите дона первым |

---

## Чего калькулятор не делает

- Не предсказывает % побед (нет Monte-Carlo).
- Не гоняет повтор визита доктора/путаны по ночам числом — только качественно.
- Нет отдельного «индекса качелей»: смотрите размах минут min–max.
- Скрытое число мафии — вилка M / M+1, не совет по умолчанию.

---

## Версии

| | |
|---|---|
| 2026-08-29 | Документация весов ролей и модификаций. htmlpreview: ванильный один HTML. |
| 2026-08-28 | Якорь ФИИМ 10/3. Маньяк вместо 1 мафии. Вторая пуля в минутах. Отравитель. Тайминг вместо «ритма». |

# Калькулятор стола — документация

Страница: https://htmlpreview.github.io/?https://github.com/YODARkiss/mafia-balance-calc/blob/main/index.html
Репозиторий: https://github.com/YODARkiss/mafia-balance-calc
Текущая модель: **v10**.

Не симулятор и не % побед. Две шкалы: **ритм** (главное) и **сила города** (жирнее / ровно / под прессом). Десятые — в спойлере и на серых +/− слотов.

## Как пользоваться

1. Игроки и секунды на речь.
2. Ритм до «нормы» (48–72 мин при речи 60 с).
3. Выключенный слот серым показывает, что будет если включить.
4. Силу трогать, только если бейдж не «примерно ровно».
5. Один чекбокс за раз.

Кнопка **«Как в Синдикате»** — таблица клуба, не оптимум калькулятора. На 12 она **снимает доктора**: у них доктор с 13.

| Игроков | Мафия | Шериф | Путана | Доктор | Маньяк |
|---|---|---|---|---|---|
| ≤10 | 2 | да | да | нет | да |
| 11–12 | 3 | да | да | нет | да |
| 13+ | 4 | да | да | да | да |

`clubM(N) = 2 если N≤10; 3 если N≤12; 4 иначе`.

## Обозначения

- N — игроки
- M — мафия (дон входит в M)
- third — 0 нет / 1 маньяк / 2 якудза
- extra — 1 если есть третья сторона
- T = N − M − third — не мафия и не третья сторона
- speech — секунд на речь

Дом: шериф стреляет и не проверяет одного дважды; дон так же; роли вскрываются; путана и доктор не себя и не к одному подряд; везунчик — мирный, один сейв; маньяк бьёт сначала мафию, шерифа не держит, победа на тройке; якудза — двое, своя банда.

## Ритм

### Блиц
Ночь снимает 1+extra, день вешает мирного.

    minR = max(1, ceil((T − M) / (2 + extra)))

12/3/маньяк: T=8, ceil(5/3)=**2**. Без маньяка: ceil(5/2)=**3**.

### Паритет

    parity = max(minR, T − M)

12/3/маньяк: **5**.

### Потолок
Не «пустые ночи до N−3».

    voteCap = N − 3
    stall   = 1·lucky + 1.9·doctor + 0.45·(путана и extra) + 0.3·(extra и M)
    maxR    = clamp(min(voteCap, parity + stall + 0.8), parity, voteCap)

Доктор не себя → +1.9, не +4. Везунчик +1. Две пули в одного редко → +0.3. Подряд к одному: пул N−2, штраф внутри stall. 12/3/маньяк/путана: maxR ≈ **6.6**, не 9.

### Ожидание

    rounds = parity + putTime + 0.4·doctor + 0.5·lucky − 1.05·sheriff − 1.2·extra
    rounds = clamp(rounds, minR, maxR)

putTime без маньяка: M/(N−1)×1.15. С маньяком: 0.4 + M/(N−1)×0.2.

12/3/маньяк/шериф/путана: rounds = 5 + 0.45 − 1.05 − 1.2 = **3.2**.

### Минуты

    речи_дня_d = max(3, N − 1 − 2(d−1)) × speech
    ночь_сек   = 62 + 15·путана + 10·дон + 15·доктор + 18·шериф + 18·extra
    минуты     = (80 + Σречи + R×165 + (R+1)×ночь_сек) / 60

165 с = 2 мин общее + ~45 с голосование. 80 с — раздача.

### Градация при речи 60 с

< 38 блиц · 38–47 коротко · **48–72 норма** · 73–88 длинно · ≥89 марафон.

Речь 45 с сжимает минуты, не трогая раунды и силу.

## Сила города

Плюс — город. На экране три слова, не «+0.79».

    p = clubM(N) − M
      + 0.85 шериф
      + putForce
      + 0.38 доктор без путаны  /  +0.22 с путаной
      + 0.20 везунчик
      − 0.15 дон
      − 0.45 маньяк
      − 0.70 якудза

    p > 0.55  город жирнее
    p < −0.55 город под прессом
    иначе     примерно ровно

putForce без маньяка: 0.3 + M/(N−1)×0.65.
С маньяком: 0.25 + P(два трупа)×0.45, где P = max(0,(N−M−2)/(N−1)).

Два трупа → она не закрыла ни одну пулю → цель мирный.

Пример 12/3/маньяк/шериф/дон/путана:

    P(два трупа)=(12−3−2)/11=7/11≈64%
    putForce=0.25+0.64×0.45≈0.54
    p=0+0.85+0.54−0.15−0.45=0.79 → город жирнее

Порог ±0.55 широкий нарочно: точное «+0.79» врёт сильнее, чем бейдж.

## Инфо ночью

Путана без маньяка: P=M/(N−1), может подсказать.
Путана с маньяком: 2 трупа — цель мирный; 1 труп — убийца, но кто — не ясно.
Доктор без путаны: тихая ночь — спасённый был под пулей.
Доктор с путаной: тишина двусмысленна.
Шериф без повторов: первая чёрная ≈ N/(M+1) ночь (на 12/3 около 3-й, не 25% каждую ночь).
Подряд к одному: ночь 1 — N−1; ночь 2+ — N−2 (~10% на 12).

## Делать / не делать

Делать: сначала ритм, потом сила; шериф — плюс городу; маньяк/якудза — минус городу; на 12 база 3 мафии + шериф + путана ± маньяк.

Не делать: % побед; сравнивать шерифа с паритетом на одной оси; потолок N−3; 4 мафии+маньяк+якудза на 12; доктор+путана на длинном вечере; считать путану с маньяком нулевым слотом.

Не считает намеренно: винрейт, блок мафии как тумблер, вечный stall «две пули всегда в одного», самолечение, отдельную шкалу маньяка.

## Версии

v1–v3 монте-карло и сломанный % · v4 часы · v5 ось 2–4–9 · v6 минуты · v7 сила и темп раздельно · v8 маньяк в силе города · v9 ритм главнее, 1/маньяк 2/якудза · **v10** серые +/−, инфо 2 трупов, кнопка Синдиката, эта методичка.
